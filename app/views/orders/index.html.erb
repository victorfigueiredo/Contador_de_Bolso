<p id="notice"><%= notice %></p>

<h1>Pedidos</h1>

<table>
  <thead>
    <tr>
      <th>Qantidade de pessoa</th>
      <th>Genero</th>
      <th>Idade</th>
      <th>Data</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.qpessoa %></td>
        <td><%= order.genero %></td>
        <td><%= order.idade %></td>
        <td><%= order.data %></td>
        <td><%= link_to 'Mostrar', order %></td>
        <td><%= link_to 'Editar', edit_order_path(order) %></td>
        <td><%= link_to 'Excluir', order, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<%= link_to 'Fazer novo pedido', produtos_path %>
<%= link_to 'Pagina inicial', root_path, method: :get %>
