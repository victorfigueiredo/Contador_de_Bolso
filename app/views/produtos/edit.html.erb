<h1>Editing Produto</h1>

<%= render 'form', produto: @produto %>

<%= link_to 'Mostrar', @produto %> |
<td><%= link_to 'Exclir', @produto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<%= link_to 'Voltar', produtos_path %>
